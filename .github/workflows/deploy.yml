name: Deploy Project
on: push

jobs:
    lint:
      runs-on: ubuntu-latest
      steps:
        - name: lint
          run: npm run lint
        - name: Simulate
          run: echo "Linting"

    test:
      runs-on: ubuntu-latest
      steps:
        - name: test
          run:  npm run test
        - name: Simulate
          run: echo "Testing"

    build:
      runs-on: ubuntu-latest
      steps:
        - name: Deploy
          run: npm run build
        - name: Simulate
          run: echo "Building"
